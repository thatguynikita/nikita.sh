# OS cruft
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node — scripts/build.mjs and scripts/deploy.mjs have zero npm
# dependencies today, but keep this ready in case tooling grows.
node_modules/
npm-debug.log*

# Local secrets — never in this repo.
*.env

# Claude Code local session state (personal, not shared)
.claude/settings.local.json
